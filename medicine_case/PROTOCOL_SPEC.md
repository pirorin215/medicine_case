# BLE通信プロトコル仕様

## 概要

このドキュメントは、スマート薬ケースシステムのBLE通信プロトコルを定義します。

## Service UUID

```
4fafc201-1fb5-459e-8fcc-c5c9c331914d
```

## Characteristic UUIDs

| UUID | プロパティ | 用途 |
|-----|-----------|------|
| `beb5483e-36e1-4688-b7f5-ea07361b26a0` | Write | コマンド送信 |
| `beb5483e-36e1-4688-b7f5-ea07361b26a2` | Notify | 応答通知 |
| `beb5483e-36e1-4688-b7f5-ea07361b26a3` | Notify | センサデータ通知 |

## 通信フロー

### コマンド・レスポンス通信

スマートフォンからコマンドを送信し、マイコンが応答を返すパターンです。

#### 1. 時刻同期

- **コマンド**: `SET:time:<unix_timestamp>`
- **説明**: マイコンに現在時刻を設定する
- **パラメータ**: Unixタイムスタンプ（秒単位）
- **応答**: `OK:time`
- **エラー**: `ERR:time`
- **タイミング**: BLE接続確立時に1回のみ実行（再接続時に再同期）

#### 2. 検出角度設定

- **コマンド**: `SET:detection:angle:<degrees>`
- **説明**: 服薬検出の角度閾値を設定する
- **パラメータ**: 角度（度数、浮動小数点）
- **デフォルト**: 70.0度
- **応答**: `OK:angle:<degrees>`
- **エラー**: `ERR:angle` (範囲: 10-180度)

#### 3. クールダウン設定

- **コマンド**: `SET:detection:cooldown:<ms>`
- **説明**: 連続検出を防ぐクールダウン時間を設定する
- **パラメータ**: クールダウン時間（ミリ秒）
- **デフォルト**: 30000ms (30秒)
- **応答**: `OK:cooldown:<ms>`
- **エラー**: `ERR:cooldown` (範囲: 1000-300000ms)

#### 4. INTAKE取得

- **コマンド**: `GET:intake`
- **説明**: マイコンに保存されている最新のINTAKEタイムスタンプを取得する
- **パラメータ**: なし
- **応答**:
  - `INTAKE:<unix_timestamp>`: 服薬イベントあり
  - `NONE`: 服薬イベントなし（時刻未同期または未検出）
- **タイミング**: BLE接続確立時に自動実行（再接続時にも自動実行）

#### 5. INTAKEクリア

- **コマンド**: `CLR:intake`
- **説明**: マイコンに保存されているINTAKEタイムスタンプをクリアする
- **パラメータ**: なし
- **応答**: `OK:clr`
- **仕様**: マイコン側は服薬検知情報を保持し続けます。スマホアプリ側ではCLR:intakeを使用せず、重複排除を実装することでデータ消失を防ぎます。

#### 6. バージョン取得

- **コマンド**: `GET:version`
- **説明**: ファームウェアバージョンを取得する
- **パラメータ**: なし
- **応答**: `OK:version:<major>.<minor>.<patch>`

### イベント通知

マイコンからの能動的な通知です。

#### INTAKEイベント通知

- **トリガー**: マイコンが服薬を検出した場合
- **フォーマット**: `INTAKE:<unix_timestamp>` または `INTAKE:0`
- **Characteristic**: Sensor characteristic
- **即時送信**: 検出直後にBLE通知が送信される

**注意点**:
- 時刻同期前の場合: `INTAKE:0` として通知
- スマホ側でtimestamp=0の場合は受信時刻を使用して補完

### 接続時のシーケンス

1. BLE接続確立
2. サービスディスカバリー
3. 通知有効化（Response + Sensor）
4. 時刻同期（`SET:time`）
5. バージョン取得（`GET:version`）
6. INTAKE取得（`GET:intake`）

**注記**: 上記シーケンスは初回接続時だけでなく、再接続時にも自動実行されます。

### 定期通信

- 接続維持中は特になし
- 必要に応じてコマンド送信
- INTAKEイベントは即座に通知

---

## エラーハンドリング

### コマンド認証エラー

- 不明なコマンドを受信した場合: `ERR:unknown`

### パラメータエラー

| エラー | 応答 | 説明 |
|--------|------|------|
| 不正なパラメータ（汎用） | `ERR:param` | パラメータ形式が不正 |
| 不正なタイムスタンプ | `ERR:time` | timestampの形式が不正 |
| 不正な角度値 | `ERR:angle` | 角度が範囲外（10-180度） |
| 不正なクールダウン値 | `ERR:cooldown` | クールダウンが範囲外（1000-300000ms） |
| 不正な設定タイプ | `ERR:config` | detection config typeが不明 |

---

## 注意事項

1. **INTAKE記録**: マイコンは最新の1つのINTAKEのみ保持します。古い記録は新しいINTAKE検出で上書きされます。

2. **重複排除**: スマホ側でINTAKE受信時にデータベースを照会し、同じmcuタイムスタンプが既に記録済みであればスキップします。これにより、通信エラー等によるデータの二重記録を防ぎます。スケジュール変更で記録がリセットされた場合は、同一タイムスタンプでも再記録されます。

3. **日付チェック**: スマホ側で日付を確認し、昨日以前のINTAKEタイムスタンプは無視します。これにより、前日の服薬検知が誤って記録されるのを防ぎます。

4. **電源管理**: BLE通信はバッテリー消費を伴うため、頻繁な通信は避けてください。

---

## 仕様変更履歴

- **v1.1.4**: 応答メッセージを短縮（MTU問題回避のため、すべて23バイト以内に収める）。設定範囲の拡大（角度: 180度, クールダウン: 5分）。
- **v1.1.3**: Response characteristicの使用ミスを修正、時刻同期前のINTAKE記録に対応
- **v1.1.2**: 時刻同期チェックを追加
- **v1.1.1**: ファームウェア分割（単一ファイル→モジュール化）
- **2026-05-27**: スマホ側で重複排除ロジックを実装（データ消失防止のためCLR:intakeコマンドを使用せず）
- **2026-05-28**: 重複排除をメモリセットからDBベースに変更。スケジュール変更時の再記録に対応。過ぎた枠の通知をスキップするロジックを追加（アクティブ枠ルール）
