# ユーザー設定モデルのシンプル化・再設計プラン (Simplified Setup Plan)

## 1. 背景と目的

現在のシステムは、服薬スケジュールを「開始時刻」と「終了時刻」という「期間」で管理しています。これは判定ロジックとしては非常に堅牢ですが、ユーザーにとっては以下の負担があります。

- **設定の煩雑さ**: 3つの時間枠に対し、合計6つの時刻を入力・調整する必要がある。
- **直感との乖離**: ユーザーは通常「12時に薬を飲む」という「点」で考え、「12時から17時の間に飲む」という「面」では考えにくい。

本プランでは、ユーザーには「点」での設定を提供し、システムが内部で「面（判定ロジック）」を自動構築する、よりユーザーフレンドリーなモデルへの刷新を検討します。

---

## 2. 提案するシンプル設定モデル

### ユーザーが見る設定項目
ユーザーは以下の最小限の項目のみを設定します。

1.  **朝の服薬時刻**: 例 `08:30` (ON/OFF切替あり)
2.  **昼の服薬時刻**: 例 `12:30` (ON/OFF切替あり)
3.  **夜の服薬時刻**: 例 `19:00` (ON/OFF切替あり)
4.  **通知の有効時間帯**: 例 `07:00 〜 23:00`
    *   この時間外には（たとえ飲み忘れがあっても）通知を行わない「安眠・プライバシー保護」の範囲。

---

## 3. 内部ロジックへの変換（自動計算）

ユーザーが入力した「点」の設定を、現在の頑健な「期間」モデルへ以下のように自動マッピングします。

### 3.1 服薬判定スロット（境界線）の自動決定
各設定時刻の中間地点を自動的にスロットの境界（開始・終了）とします。

*   **例**: 朝 08:00、昼 12:00、夜 20:00 の場合
    *   **朝スロット**: 04:00（固定） 〜 10:00（朝と昼の中間）
    *   **昼スロット**: 10:00 〜 16:00（昼と夜の中間）
    *   **夜スロット**: 16:00 〜 04:00（固定）

これにより、ユーザーがいつケースを傾けても、最も近い設定時刻のスロットに自動的に振り分けられます。

### 3.2 通知開始タイミングの自動決定
「目標時刻」を過ぎたら即座に、あるいは一定の猶予（バッファ）を持って通知を開始します。

*   **案**: 目標時刻の **N分後**（例：30分後や60分後、あるいは設定で変更可能）を内部的な「終了時刻（デッドライン）」として扱う。
*   このバッファ期間内は、前述の「インテリジェント・リマインダー（BLE接続時のチャンス通知）」が機能する期間となります。

---

## 4. ユースケース別の挙動

### ユースケース 1: 1日1回（昼のみ）
*   設定：昼 12:00 (ON)、朝・夜 (OFF)
*   内部挙動：
    *   04:00 〜 翌04:00 までがすべて「昼スロット」として扱われる。
    *   12:00 を過ぎるまでは通知なし。
    *   12:00 〜 13:00（バッファ）の間は、BLE接続時のみ「今のうちにどうぞ」通知。
    *   13:00 を過ぎると「飲み忘れ」として正式通知。

### ユースケース 2: 朝・夜のみ
*   設定：朝 08:00 (ON)、夜 20:00 (ON)、昼 (OFF)
*   内部挙動：
    *   境界は 14:00（8時と20時の中間）。
    *   04:00 〜 14:00 が「朝スロット」。
    *   14:00 〜 04:00 が「夜スロット」。

---

## 5. 検討が必要な課題（妥協点）

1.  **通知の「安眠範囲」と「スロット」の競合**:
    *   23:00で通知範囲が終了する場合、夜のスロットのデッドラインはどうあるべきか。
2.  **深夜・早朝の扱い**:
    *   日付を跨ぐ設定（夜 25:00 など）を許容するか。
3.  **移行の容易さ**:
    *   現在の Room / DataStore の構造を大きく変えずに、変換レイヤーだけで対応できるか。

---

## 6. 実装のステップ案

1.  **設定データ構造の変更**: `AppSettingsData` に `preferredTime` 系のフィールドを追加。
2.  **Repositoryでの変換ロジック実装**: シンプル設定から `MedicineSchedule` オブジェクト（期間モデル）を生成する関数を作成。
3.  **UIの刷新**: タイムピッカーとON/OFFスイッチだけのシンプルな画面へ変更。
4.  **通知・記録ロジックの接続**: 内部ロジックは「計算された期間」をそのまま使うため、最小限の変更で済むはず。
